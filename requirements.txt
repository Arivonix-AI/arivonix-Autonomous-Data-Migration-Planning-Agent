SQLAlchemy>=2.0
pydantic>=2.5
PyYAML>=6.0
python-dotenv>=1.0

# Optional - enables LLM-synthesized narrative, risk refinement and
# ambiguous-mapping resolution. The agent runs fully offline without it.
anthropic>=0.40.0

# Dev/test
pytest>=8.0
pytest-cov>=5.0
