# Optional. When set, the agent uses Claude to synthesize the migration
# narrative, refine risks, and resolve ambiguous column mappings.
# Without it, the agent falls back to a fully offline heuristic synthesizer.
ANTHROPIC_API_KEY=

# Optional override, defaults to a current Claude model.
ANTHROPIC_MODEL=claude-sonnet-5
